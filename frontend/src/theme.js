import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
        main: '#fff', // set your default background color here
      },
      text: {
        default: '#fff', // set your default text color here
      },
  }
});

export default theme;
